# PyTorch 与 torchvision 请按目标服务器的 CUDA 12.8+ wheel 单独安装，避免被 CPU wheel 覆盖。
numpy>=1.26
opencv-python>=4.8
PyYAML>=6.0
tqdm>=4.66
tensorboard>=2.14
natsort
lpips==0.1.4
# 固定官方 THOP 版本，避免解析到同名 fork 或后续不兼容实现。
thop==0.1.1.post2209072238
